Django>=4.2,<5.1
djangorestframework==3.15.2
django-cors-headers==4.4.0
drf-yasg==1.21.7
mysqlclient

# Email libraries (built-in Django)
# لا نحتاج مكتبات إضافية للإيميل

# ✅ Celery & Redis - للمهام الخلفية (Async Processing)
celery[redis]==5.4.0
redis==5.0.1
flower==2.0.1  # لمراقبة Celery tasks
django-celery-results==2.5.1
django-celery-beat==2.7.0

# Utilities
requests==2.31.0
python-dotenv==1.0.0

# Development only (اختياري)
# pytest==7.4.3
# pytest-django==4.7.0
# faker==20.1.0
